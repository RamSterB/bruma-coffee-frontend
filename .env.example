# URL base de la API. Se usa como target del proxy /api de Vite en desarrollo.
# host.docker.internal resuelve al host (Docker Desktop) donde se publica el backend.
VITE_API_URL=http://host.docker.internal:8000